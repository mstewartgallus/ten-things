import { Section, Header, H1, P } from "@/ui";

const Loading = () =>
    <Section
       heading={
          <Header>
              <H1>Loading...</H1>
          </Header>
       }>
       <P>Loading...</P>
    </Section>;

export default Loading;
